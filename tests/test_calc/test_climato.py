# import huracanpy

# import numpy as np


def test_climato():
    """
    data = huracanpy.load(
        huracanpy.example_csv_file,
    )

    # get_freq
    freq = huracanpy.calc._climato.get_freq(data.track_id)
    assert freq == 3

    # get_tc_days
    tc_days = huracanpy.calc._climato.get_tc_days(time=data.time, track_ids=data.track_id)
    np.testing.assert_approx_equal(tc_days, 26.25, significant=4)
    """
    pass
